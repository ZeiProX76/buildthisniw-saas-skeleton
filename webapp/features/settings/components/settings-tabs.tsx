'use client'

/**
 * SettingsTabs — vertical sidebar on desktop, horizontal list on mobile.
 * Controls the ?tab= URL param via nuqs. OUTSIDE Suspense — always interactive.
 * Checks form dirty state before navigating (unsaved changes warning).
 */

export function SettingsTabs() {
  return <div data-slot="settings-tabs">TODO: designer</div>
}
