/**
 * BillingSection — read-only current plan display + "Manage billing" link to Stripe portal.
 * Server component. No mutations. Designer fills plan display + CTA.
 */
export async function BillingSection() {
  return <div data-slot="billing-section">TODO: designer</div>
}
